services:
  - type: web
    name: my-python-app
    runtime: python
    buildCommand: pip install -U discord.py
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: PORT
        value: 5000
